name: Symfony API CI

on:
  schedule:
    -   cron: '0 2 * * *'  # Tous les jours à 02:00 UTC
  push:
        branches: [ "main" ]
#  pull_request:
#    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php-version: ['8.5']

    steps:
      # 1️⃣ Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

     # 2️⃣ Delete old artifacts
      - name: Clean old GitHub Actions artifacts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh auth setup-git
          artifacts=$(gh api repos/${{ github.repository }}/actions/artifacts | jq -r '.artifacts[].id')
          for artifact in $artifacts; do
            gh api -X DELETE repos/${{ github.repository }}/actions/artifacts/$artifact
          done

      # 2️⃣ Installer PHP
      - name: Setup PHP
        uses: shivammathur/setup-php@44454db4f0199b8b9685a5d763dc37cbf79108e1
        with:
          php-version: ${{ matrix.php-version }}
          extensions: mbstring, intl, pdo_mysql, xml, curl
          coverage: xdebug

      # 3️⃣ Installer Composer
      - name: Install Composer dependencies
        run: composer install --prefer-dist --no-progress --no-interaction

      # 4️⃣ Valider le composer.json
      - name: Composer validate
        run: composer validate --no-check-publish

      # 5️⃣ Vérifier le style de code
      - name: PHP-CS-Fixer dry-run
        run: ./vendor/bin/php-cs-fixer fix --dry-run --diff

      # 6️⃣ Analyse statique avec PHPStan
      - name: PHPStan analysis
        run: ./vendor/bin/phpstan analyse -l 8 src

      # 7️⃣ Lancer les tests unitaires avec PHPUnit
      - name: Run PHPUnit
        run: ./vendor/bin/phpunit --configuration phpunit.dist.xml --coverage-clover=coverage.xml

      # 8️⃣ Rector dry-run
      - name: Rector dry-run
        run: ./vendor/bin/rector process src --dry-run

